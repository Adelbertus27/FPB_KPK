function fpb_kpk(x,y) {
    for(i=1;i<=x;i++){
        fpb = Math.floor(i) 
        console.log('FPB ' + x + ' & ' + y +' = '+Math.floor(fpb))
        for(i=1;i<=y;i++){
            }  
    } kpk = Math.max(x) * Math.max(y)
    console.log('KPK ' + x + ' & ' + y +' = '+kpk)
}

fpb_kpk(2,3)